<UserControl x:Class="EthernetPacketGenerator.Views.ProtocolPaletteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel Background="{StaticResource SurfaceBrush}">
        <TextBlock DockPanel.Dock="Top" Text="PROTOCOLS" Style="{StaticResource PanelHeader}"/>
        <TextBlock DockPanel.Dock="Top"
                   Text="클릭하여 추가 / 드래그하여 삽입"
                   FontSize="9" Foreground="{StaticResource MutedForeground}"
                   Margin="6,2,6,4"/>

        <ListBox ItemsSource="{Binding PaletteItems}"
                 Background="Transparent" BorderThickness="0"
                 PreviewMouseLeftButtonDown="Palette_MouseDown"
                 PreviewMouseMove="Palette_MouseMove"
                 PreviewMouseLeftButtonUp="Palette_MouseUp"
                 x:Name="PaletteList">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="4,3"/>
                    <Setter Property="Cursor" Value="Hand"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border Background="{TemplateBinding Background}"
                                        CornerRadius="4" Padding="0">
                                    <ContentPresenter/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#33FFFFFF"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Width="150" Height="40" CornerRadius="4" Margin="0"
                            Background="{Binding ProtocolType, Converter={StaticResource ProtoToColor}}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="44"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0"
                                       Text="{Binding ProtocolType, Converter={StaticResource ProtoToAbbrev}}"
                                       FontWeight="Bold" FontSize="12" Foreground="White"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{Binding DisplayName}"
                                           FontSize="11" FontWeight="SemiBold" Foreground="White"/>
                                <TextBlock Text="{Binding Description}"
                                           FontSize="9" Foreground="#CCDDFF" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </DockPanel>
</UserControl>
